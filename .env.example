NODE_ENV=production
PORT=3001
DATABASE_PATH=/app/data/app.db     # Chemin absolu dans le container — ne pas modifier
SESSION_SECRET=                     # Min 32 chars — générer avec : openssl rand -hex 32
ALLOWED_ORIGINS=https://tonsite.com # Liste séparée par virgules si plusieurs origines
